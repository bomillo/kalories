name: kalories
description: Simple app for counting nutritional values intake during the day.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.2+1
  sqflite_common_ffi: ^2.1.1
  tuple: ^2.0.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
